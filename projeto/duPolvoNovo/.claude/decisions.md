## [2026-07-09] Animação de scroll da seção "solução": GSAP + ScrollTrigger via CDN, não bundler

**Decisão:** Seção `#solucao` (etapas do processo) ganhou uma timeline pinada (`scrollTrigger:{pin:true,scrub:0.6}`) com barra de progresso via `transform:scaleX()` e 4 etapas ativando em sequência (classe `.step--active`). GSAP 3.15.0 + ScrollTrigger carregados via `<script>` do unpkg, no mesmo padrão do Lucide já usado no site — não via npm/bundler.
**Motivo:** `duPolvoNovo/duPolvo/` é um site estático vanilla JS (sem build step, sem `package.json`), então CDN é a única forma de trazer o GSAP sem reestruturar o projeto. A stack oficial de Tipo A (`projeto/CLAUDE.md`) prevê Astro + GSAP via npm — esse site ainda não migrou pra Astro (ver pendência registrada no `CLAUDE.md` raiz), então a decisão aqui é uma ponte até essa migração acontecer, não o padrão final.
**Alternativas descartadas:** Animar a linha de progresso com SVG `stroke-dashoffset` (efeito de "desenhar" mais literal) — rejeitada por violar a regra de só animar `transform`/`opacity` (stroke-dashoffset tem custo de repaint, não é compositor-only). Também descartado usar `gsap.to()` com `transform:scale` direto no `.step__ic` teria conflitado com o hover em CSS (`transform` inline do GSAP sobrepõe a transição de hover) — resolvido usando GSAP só pra pin/scrub/timeline, e classes CSS (`.step--active`) pro estado visual de cada etapa, mantendo o hover intacto.
**Impacto:** Estado inicial "escondido" dos ícones/textos só existe atrás da classe `.motion-ready`, adicionada via JS somente quando GSAP+ScrollTrigger carregam e `prefers-reduced-motion` não está ativo — sem JS ou com motion reduzido, a seção sempre mostra o estado final, sem tela vazia. Verificado com Playwright MCP em 2026-07-10 (ver decisão abaixo — bug encontrado e corrigido no mesmo dia).

## [2026-07-10] Linha de progresso das etapas: pin+scrub só em desktop (>1024px), trigger independente por etapa em tablet/mobile

**Decisão:** A timeline pinada com barra mestra horizontal (`.steps__line-fill`, `scaleX`) só faz sentido no grid de 4 colunas numa fileira só (desktop, >1024px) — abaixo disso o grid quebra pra 2 colunas (tablet, 761–1024px) ou 1 coluna (mobile, ≤760px), e essa barra horizontal fixa (`top:30px;left:8%;width:84%`) nunca alcança as etapas da 2ª fileira/linhas seguintes. Isso fazia só a Etapa 01 (ou a 1ª fileira, no tablet) parecer "funcionar" — não era um bug pontual da Etapa 01, era a suposição de layout horizontal quebrando abaixo de 1024px. Trocado pra: cada `.step` ganha sua própria linha, autocontida, com seu próprio `ScrollTrigger` (sem pin, `start:'top 75%'`, `end:'top 30%'`, `scrub:0.6`) disparando de forma independente conforme aquela etapa entra na área visível. Implementado com `gsap.matchMedia()` — `(min-width:1025px)` mantém a timeline pinada original intacta, `(max-width:1024px)` roda os 4 triggers independentes.

**Correção 2026-07-10 (mesmo dia):** a 1ª versão dessa linha por-etapa era vertical (`scaleY`, conectando o ícone de cima ao de baixo) — o usuário apontou que isso não era o padrão pedido: o comportamento original da Etapa 01 (que já funcionava) era uma linha **horizontal curta ao lado do ícone**, preenchendo da esquerda pra direita, não uma linha vertical conectando ícones. Corrigido pra `.step__line{top:31px;left:76px;width:90px;height:2px}` (curta, alinhada ao centro vertical do ícone, começando logo à direita dele) com `.step__line-fill{transform-origin:left;scaleX}` — mesma orientação da barra mestra do desktop, só que curta e por etapa em vez de uma única barra longa.
**Motivo:** Pin numa lista vertical longa (2 ou 4 cards empilhados) é ruim de usar em touch — trava o scroll do usuário por mais tempo do que o necessário. O padrão por-etapa também é mais robusto a qualquer altura de conteúdo (não depende de alinhar pixel a pixel com a próxima etapa). A orientação horizontal (não vertical) foi decisão explícita do usuário, replicando o visual já validado da Etapa 01.
**Alternativas descartadas:** Reorientar a barra mestra única pra vertical (mesma barra, só trocando `scaleX`→`scaleY` e width/height) — descartada porque ainda exigiria calcular a altura exata até a última etapa dinamicamente (grid tem alturas variáveis por causa do texto), e manteria o pin numa lista vertical longa, ruim em mobile. Linha vertical por etapa (1ª tentativa) — descartada por feedback direto do usuário, não era o padrão visual pedido.
**Impacto:** `index.html` (novo `<span class="step__line">` dentro de cada `.step`), `css/style.css` (`.step__line`/`.step__line-fill` horizontal e curta nos breakpoints ≤1024px, `.steps__line` escondida nesse range), `js/main.js` (bloco da seção solução reescrito com `gsap.matchMedia()`, `scaleX` em vez de `scaleY`). Verificado com Playwright em scroll real (mouse-wheel, não `scrollTo`) nos 3 breakpoints: mobile 375px, tablet 820px, desktop 1440px.

## [2026-07-10] overflow-x:hidden precisa estar em `html`, não só em `body`

**Decisão:** Adicionado `overflow-x:hidden` na regra `html{}` (que já tinha `scroll-behavior:smooth`), além do `overflow-x:hidden` que já existia em `body{}`.
**Motivo:** O ticker/marquee da hero (`.ticker`) tem `transform:rotate(-1.6deg) scale(1.045)` pra dar o efeito "faixa colada torta" — a rotação faz a própria caixa do elemento (não só os filhos) vazar horizontalmente além da largura original, e `overflow:hidden` no próprio elemento só recorta os filhos dele, não a si mesmo. Em mobile isso aparecia como o texto do ticker sendo cortado na borda direita da tela. `body{overflow-x:hidden}` sozinho não fecha esse vazamento porque o scroll horizontal do documento em navegadores mobile (particularmente iOS Safari) é controlado pelo elemento `html`, não pelo `body` — confirmado medindo `document.documentElement.scrollWidth` (556px) vs `clientWidth` (360px) antes da correção, e iguais depois.
**Alternativas descartadas:** Envolver o `.ticker` num wrapper extra com `overflow-x:hidden` — desnecessário, já que ele é filho direto do `body`; corrigir no `html` resolve na raiz sem markup extra.
**Impacto:** Durante a varredura de página inteira (checando `getBoundingClientRect()` de todo elemento contra a viewport) também apareceu `.step__line{width:460px}` no breakpoint `max-width:760px` (editado depois da decisão anterior) — mais largo que qualquer tela de celular, cortando a linha de progresso na borda. Removida essa sobrescrita fixa, voltando a herdar o `clamp(180px,45vw,280px)` já definido no breakpoint de 1024px. Verificado com Playwright em mobile (375px): `document.documentElement.scrollWidth === clientWidth` (sem overflow real), e os únicos elementos que ainda "vazam" em `getBoundingClientRect()` são decorativos por design (`hero__glow`, `cta__glow`, `ticker__track`) — todos contidos visualmente, sem scroll horizontal possível.

## [2026-07-17] Intro splash: entrada por tentáculo (100% transform/opacity), sem stroke-draw nem clip-path

**Decisão:** Adicionado overlay de abertura (`#intro`) que roda 1x por sessão antes do conteúdo, usando `assets/logo.svg` inline (não `<img>`, pra poder animar path a path). Entre 3 conceitos apresentados (traço se desenhando via `stroke-dashoffset`/DrawSVG, máscara circular via `clip-path`, distorção líquida via filtro SVG), o usuário escolheu o único 100% compositor-only: o corpo da logo (`.intro__body`, fill rosa) escala+aparece primeiro, as 9 ventosas (`.intro__spot`, fill aqua) entram em stagger de 60ms tipo bolha, um pulse de assentamento fecha a composição, e a saída é uma cortina dupla (`.intro__panel--l/--r`, `xPercent:±100`) revelando a hero. Timeline GSAP, ~2.7s total (orçamento era 2–3.5s). `sessionStorage` (`dupolvo:intro-seen`) evita repetir na mesma sessão; `prefers-reduced-motion` pula direto pro conteúdo (checado de forma síncrona antes do markup da intro ser parseado, sem flash); `<noscript>` esconde a intro se JS estiver desligado; keyframe CSS puro (`introFailsafe`, 4.2s) força o desaparecimento mesmo se o JS quebrar no meio do caminho. `heroEntrance()` só dispara quando a intro termina (ou imediatamente, se ela for pulada) — antes disso o hero já estava totalmente revelado antes da intro sequer sumir.
**Motivo:** Os dois outros conceitos (`stroke-dashoffset`/DrawSVG e `clip-path`) violam a regra fixa do projeto de animar só `transform`/`opacity` — `stroke-dashoffset` tem custo de repaint, `clip-path` só tem aceleração de GPU parcial e não é garantidamente compositor-only. O padrão "por tentáculo" (corpo → ventosas em stagger → pulse) além de ser 100% compositor-only, também é on-brand com a copy que já existe no site ("oito braços", "tentáculos").
**Alternativas descartadas:** Traço se desenhando (mais literal, mas exige stroke em vez de fill — e os paths da logo são preenchidos, não traçados) e distorção líquida via `feTurbulence`/`feDisplacementMap` (mais expressivo/aquático, mas SVG filter é caro de renderizar e o maior risco de jank em Android mid-range) — ambos descartados pelo usuário em favor da opção mais segura de performance.
**Impacto:** `index.html` (markup do `#intro` com SVG inline + script síncrono no topo do `<body>`), `css/style.css` (bloco INTRO SPLASH + `@keyframes dupFloat`/`introFailsafe`), `js/main.js` (`runIntro()` e mudança do Boot pra `runIntro(heroEntrance)`). Verificado com Playwright (desktop 1440px e mobile 375px, intro completa + skip por sessionStorage + skip por `prefers-reduced-motion` via `page.emulateMedia`) e trace de performance via Chrome DevTools (LCP 1.35s, CLS 0.00, sem jank sinalizado).

## [2026-07-17] Mascote (Dup) na hero: centro do blob no desktop/tablet, abaixo do texto no mobile

**Decisão:** `assets/Dup.png` (2.3MB, 1536×1024, fundo transparente com glow suave) foi recortado (trim de transparência + 4% de respiro), redimensionado e exportado como `dup-hero.webp` (~106KB) + `dup-hero.png` de fallback (~173KB) em `duPolvo/assets/img/`. No desktop/tablet (`.hero__dup`), o Dup fica absoluto no centro do `.hero__blob`, atrás dos 4 chips de serviço (z-index 2 vs 3) — reforçando a leitura de "chips = braços do polvo". Entrada orquestrada: Dup aparece em 380ms (antes do primeiro chip, em 540ms) — o "corpo" se monta antes dos "braços". Flutuação contínua via `@keyframes dupFloat` (`translateY`, só `transform`), com handoff sem salto entre a transição de entrada (700ms) e o início do loop (`animation-delay:.7s`), mesma técnica já usada nos chips. No mobile (≤760px, onde `.hero__media` já ficava com `display:none`), um segundo elemento (`.hero__dup-mobile`) — mesma imagem, menor — aparece em fluxo normal logo depois do CTA/prova social e antes do ticker.
**Motivo:** Posicionar o Dup acima do título no mobile foi descartado porque empurraria título/lead/CTA pra baixo, arriscando o botão principal sair da tela sem rolar — prioridade de conversão em mobile pede CTA acima da dobra. Dois elementos (em vez de um só repaginado via CSS Grid `order`) porque a posição do Dup no desktop é absoluta dentro do `.hero__blob` (centralização automática do container) enquanto no mobile precisa estar em fluxo normal depois do `.hero__grid` — replicar isso com um elemento só exigiria posicionamento absoluto calculado contra as frações do grid (`1.6fr .95fr`), frágil a mudanças de layout.
**Alternativas descartadas:** Dup acima do título no mobile (rejeitada por risco de esconder o CTA). Um único elemento reposicionado via `order`/`grid-column` (rejeitada por fragilidade de posicionamento absoluto fora do container que já centraliza automaticamente).
**Impacto:** `index.html` (dois `<picture>` novos), `css/style.css` (`.hero__dup`, `.hero__dup-mobile`, `@keyframes dupFloat`, breakpoint ≤760px, entrada em `prefers-reduced-motion`), `js/main.js` (`heroEntrance()` inclui `dup` no stagger). De quebra, a auditoria de acessibilidade (Lighthouse, rodada como parte do QA desta entrega) achou 3 falhas pré-existentes não relacionadas às duas adições — `color-contrast` em `.proc-item__n` (aqua claro sobre branco), `heading-order` (h2→h4 nos `.diff-row`, h2→h5 no rodapé) e `landmark-one-main` (sem `<main>`) — corrigidas juntas por serem baratas e por essa entrega ter o gate obrigatório de Accessibility=100 no `CLAUDE.md` raiz. Lighthouse final: Accessibility 100, Best Practices 100, SEO 100; LCP 1.35s e CLS 0.00 medidos via trace (sem score numérico de Performance disponível nas ferramentas MCP usadas, mas os Core Web Vitals medidos estão bem dentro do "bom").

## [2026-07-19] Dup exclusivo do desktop — removido de tablet e mobile

**Decisão:** `.hero__dup` (centro do `hero__blob`) ganhou `display:none` no breakpoint `max-width:1024px`, então some junto com o resto do `.hero__media` em tablet (761–1024px) — que antes mantinha o mascote visível, só perdendo o layout de 2 colunas mais abaixo. O elemento `.hero__dup-mobile` (versão pequena abaixo do CTA, criada na entrega anterior) foi removida por completo — markup, CSS (`.hero__dup-mobile` base, breakpoint ≤760px, entrada/`hero-in`, `prefers-reduced-motion`) e a referência no `heroEntrance()` do `js/main.js`. O Dup agora só existe (e só é renderizado no DOM) em telas >1024px.
**Motivo:** Pedido direto do usuário — reverte a decisão anterior de mostrar uma versão menor do Dup no mobile abaixo do CTA. Em vez de só esconder visualmente com CSS, optou-se por remover o elemento do `.hero__dup-mobile` do DOM inteiramente (não só `display:none`) já que ele não tinha mais nenhum uso em nenhum breakpoint — menos markup morto pra manter.
**Alternativas descartadas:** Manter o Dup visível em tablet (761–1024px) e cortar só no mobile — descartada explicitamente pelo usuário ao escolher "some em tablet e mobile" na pergunta de esclarecimento sobre onde cortar.
**Impacto:** `index.html` (remove o `<picture class="hero__dup-mobile">`), `css/style.css` (remove bloco `.hero__dup-mobile` e suas 3 ocorrências em entrada/breakpoint/reduced-motion; adiciona `.hero__dup{display:none}` em `max-width:1024px`), `js/main.js` (`heroEntrance()` agora seleciona só `.hero__dup`). Verificado com Playwright em 3 contextos (`reducedMotion:'reduce'` pra pular a intro e comparar o estado final direto) — mobile 375px, tablet 900px, desktop 1440px: Dup aparece só no desktop, chips continuam orbitando normalmente em tablet.

## [2026-07-20] GSAP/Lucide self-hosted — CDN unpkg removido

**Decisão:** Os três `<script>` de `duPolvo/index.html` que carregavam GSAP, ScrollTrigger e Lucide via `unpkg.com` agora apontam pra cópias locais versionadas em `duPolvo/js/vendor/` (`gsap-3.15.0.min.js`, `ScrollTrigger-3.15.0.min.js`, `lucide-0.460.0.min.js`), baixadas via `npm pack` (registry oficial do npm, não fetch direto do CDN) e commitadas no repo. `dns-prefetch`/`preconnect` pra `unpkg.com` removidos do `<head>` por não terem mais uso. O mesmo vale pros dois usos secundários de Lucide (não são páginas públicas): `guidelines/brand-logo.html` e `sections/preview.html` agora apontam pra `../assets/vendor/lucide-0.460.0.min.js` (cópia compartilhada na raiz do projeto, já que essas duas páginas não fazem parte do deploy de `duPolvo/`).
**Motivo:** Achado 🟠 Alto da auditoria de segurança do MazyOS (`AUDITORIA-SEGURANCA.md`, Fase 0): CDN de terceiro sem SRI em produção — se o unpkg for comprometido ou a versão for alterada, código arbitrário roda no site institucional público. Isso supersede a decisão de 2026-07-09 ("CDN é a única forma de trazer o GSAP sem reestruturar o projeto") — o motivo daquela decisão (sem `package.json`, sem build step) continua verdadeiro, mas vendorizar o arquivo estático já resolve o risco de supply chain sem precisar de bundler.
**Alternativas descartadas:** Adicionar `integrity`/`crossorigin` (SRI) mantendo o CDN — mitigaria só troca de versão pelo próprio unpkg, não cobre o cenário de unpkg fora do ar ou de política de cache do navegador servindo versão antiga; self-host resolve os dois de uma vez e elimina a dependência de disponibilidade de terceiro.
**Impacto:** `duPolvo/index.html` (3 `<script src>` + remoção do preconnect), `guidelines/brand-logo.html`, `sections/preview.html`. Novo diretório `duPolvo/js/vendor/` (72KB gsap + 44KB ScrollTrigger + 356KB lucide) e `assets/vendor/` (356KB lucide, cópia compartilhada). Verificado com Playwright (servidor estático local): `window.gsap`/`window.ScrollTrigger`/`window.lucide` carregam normalmente (`gsap.version === "3.15.0"`), zero erro de console além do 404 pré-existente de `favicon.ico`, nas 3 páginas afetadas. Atualizar a versão vendorizada no futuro exige repetir o processo manualmente (não há Dependabot cobrindo arquivo estático) — revisitar se o projeto migrar pra Astro (pendência já conhecida no `CLAUDE.md` raiz).
